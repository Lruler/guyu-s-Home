import{_ as i,p as r,q as a,s as e,R as c,t as l,Z as o,n as p}from"./framework-1749bed7.js";const s="/guyu.inn/assets/vite-c728afc7.webp",t={},n=o('<p>小小的深入学习一下vite</p><p>学习Vite之前 建议先去看一下ES Module 和 CommonJs的区别</p><h2 id="概览" tabindex="-1"><a class="header-anchor" href="#概览" aria-hidden="true">#</a> 概览</h2><p>启动</p><blockquote><p>先用 esbuild 进行预构建</p><ul><li>将非 <code>ESM</code> 规范的代码转换为符合 <code>ESM</code> 规范的代码；</li><li>将第三方依赖内部的多个文件合并为一个，减少 <code>http</code> 请求数量；</li></ul><p>然后不需要构建module graph，浏览器会根据ESM规范对各个模块发起请求，发起请求以后，<code>dev server</code> 端会通过 <code>middlewares</code> 对请求做拦截，然后对源文件做 <code>resolve</code>、<code>load</code>、<code>transform</code>、<code>parse</code> 操作，然后再将转换以后的内容发送给浏览器。</p></blockquote><p>更新</p><blockquote><p><code>dev server</code> 在监听到文件发生变化以后，只需要通过 <code>ws</code> 连接通知浏览器去重新加载变化的文件，剩下的工作就交给浏览器去做了。</p></blockquote><p>打包，通过 rollup来打包，和bundle的构建打包过程一致了。 Rollup分为build（构建）阶段和output generate（输出生成）阶段。主要过程如下：</p><ul><li>获取入口文件的内容，包装成module，生成抽象语法树（AST）</li><li>对入口文件抽象语法树进行依赖解析</li><li>生成最终代码</li><li>写入目标文件</li></ul><p>打包产物：</p><p>与webpack产物的最大区别就是没有额外的注入代码</p><p>额外代码：</p><ul><li>webpack自己的兼容代码，目的是自己实现require，modules.exports，export，让浏览器可以兼容cjs和esm语法</li><li>（可以理解为，webpack自己实现polyfill支持模块语法，rollup是利用高版本浏览器原生支持esm(所以rollup无需代码注入)）</li></ul><h3 id="tree-shanking" tabindex="-1"><a class="header-anchor" href="#tree-shanking" aria-hidden="true">#</a> Tree shanking</h3><p>Tree Shaking 是一种在构建过程中移除未使用代码（死代码）的优化技术。这个术语源于将未使用的代码想象成树上的枯萎叶子，而通过 &quot;摇动&quot;（Shaking）树来摘除这些叶子。Tree Shaking 的主要目标是减小构建产物的体积，从而提高应用程序的加载性能。</p><p>Tree Shaking 的工作原理主要依赖于以下几个方面：</p><ol><li><p>静态模块结构：Tree Shaking 依赖于 ES 模块（ECMAScript Module）的静态导入和导出语句。由于 ES 模块的依赖关系在代码解析阶段就已经确定，这使得构建工具可以准确地识别出未使用的代码。</p></li><li><p>代码分析：构建工具（如 Webpack、Rollup 等）会分析项目中的模块依赖关系，生成一个依赖图。然后，构建工具会检查依赖图中的每个模块，确定其导出的成员是否在其他模块中被使用。</p></li><li><p>移除未使用代码：构建工具会遍历依赖图，将未使用的模块成员标记为死代码。然后，在输出构建产物时，这些死代码会被移除。这样，最终的构建产物只包含实际使用的代码，从而减小了文件体积。</p></li></ol><p>需要注意的是，Tree Shaking 只能移除未使用的模块成员，而无法移除模块内部的死代码。为了进一步优化构建产物，可以结合其他代码优化技术，如代码压缩（UglifyJS、Terser 等）和作用域提升（Scope Hoisting）。</p><p>总之，Tree Shaking 是一种用于移除未使用代码的优化技术。通过使用 Tree Shaking，开发者可以减小构建产物的体积，提高应用程序的加载性能。为了充分利用 Tree Shaking，建议使用 ES 模块，并确保构建工具和配置支持 Tree Shaking。</p><h2 id="先看看vite和webpack的区别" tabindex="-1"><a class="header-anchor" href="#先看看vite和webpack的区别" aria-hidden="true">#</a> 先看看vite和Webpack的区别</h2><p><strong>webpack是先打包再启动开发服务器，vite是直接启动开发服务器，然后按需编译依赖文件。</strong></p><h5 id="下面详细来说" tabindex="-1"><a class="header-anchor" href="#下面详细来说" aria-hidden="true">#</a> 下面详细来说：</h5><ul><li>webpack先打包，再启动开发服务器，请求服务器时直接给予打包后的结果；</li><li>vite直接启动开发服务器，请求哪个模块再对哪个模块进行实时编译；</li><li>由于现代浏览器本身就支持ES Modules，会主动发起请求去获取所需文件。vite充分利用这点，将开发环境下的模块文件，就作为浏览器要执行的文件，而不是像webpack先打包，交给浏览器执行的文件是打包后的；</li><li>由于vite启动的时候不需要打包，也就无需分析模块依赖、编译，所以启动速度非常快。当浏览器请求需要的模块时，再对模块进行编译，这种按需动态编译的模式，极大缩短了编译时间，当项目越大，文件越多时，vite的开发时优势越明显；</li><li>在HRM方面，当某个模块内容改变时，让浏览器去重新请求该模块即可，而不是像webpack重新将该模块的所有依赖重新编译；</li><li>当需要打包到生产环境时，vite使用传统的rollup进行打包，所以，vite的优势是体现在开发阶段，另外，由于vite使用的是ES Module，所以代码中不可以使用CommonJs；</li></ul><p>简单点来说就是，webPack会先从你的入口文件开始，逐层寻找你的依赖，然后在统一打包编译，再由浏览器运行</p><h2 id="vite在官网上的介绍" tabindex="-1"><a class="header-anchor" href="#vite在官网上的介绍" aria-hidden="true">#</a> Vite在官网上的介绍</h2><p>选Vite 可以解决两个问题</p><ol><li>缓慢的服务器启动 开发模式下</li></ol><p>esbuild预构建 + ESM模块 no bundle 模式</p><ol start="2"><li>缓慢的更新</li></ol><p>利用websocket监听，支持了动态模块热替换（HMR）：允许一个模块 “热替换” 它自己，而不会影响页面其余部分</p><p>Vite 同时利用 HTTP 头来加速整个页面的重新加载（再次让浏览器为我们做更多事情）：源码模块的请求会根据 <code>304 Not Modified</code> 进行协商缓存，而依赖模块请求则会通过 <code>Cache-Control: max-age=31536000,immutable</code> 进行强缓存，因此一旦被缓存它们将不需要再次请求。</p><p>生产模式下还是会用rollup打包</p><blockquote><p>尽管原生 ESM 现在得到了广泛支持，但由于嵌套导入会导致额外的网络往返，在生产环境中发布未打包的 ESM 仍然效率低下（即使使用 HTTP/2）。为了在生产环境中获得最佳的加载性能，最好还是将代码进行 tree-shaking、懒加载和 chunk 分割（以获得更好的缓存）</p></blockquote><p>不用esbuild打包的原因</p><blockquote><p>Vite 目前的插件 API 与使用 <code>esbuild</code> 作为打包器并不兼容。尽管 <code>esbuild</code> 速度更快，但 Vite 采用了 Rollup 灵活的插件 API 和基础建设，这对 Vite 在生态中的成功起到了重要作用。目前来看，我们认为 Rollup 提供了更好的性能与灵活性方面的权衡。</p><p>即便如此，<code>esbuild</code> 在过去几年有了很大进展，我们不排除在未来使用 <code>esbuild</code> 进行生产构建的可能性。我们将继续利用他们所发布的新功能，就像我们在 JS 和 CSS 最小化压缩方面所做的那样，<code>esbuild</code> 使 Vite 在避免对其生态造成干扰的同时获得了性能提升。</p></blockquote><p>静态资源分为两类：预构建内容和业务代码。其中，预构建内容通常是由项目中的第三库生成的，采用强缓存策略，业务代码则采用协商缓存策略。</p><p>协商缓存，通常需要在发起请求时，在请求头中添加 <code>If-None-Match</code> 字段，携带请求文件对应的 <code>ETag</code> 信息。当 <code>dev server</code> 收到请求时，会将请求头的 <code>ETag</code> 和请求文件的 <code>ETag</code> 信息做对比，如果没有变化，返回 <code>304</code> 通知浏览器使用本地缓存，反之则返回新的文件内容。</p><p>在获取请求文件的 <code>ETag</code> 信息这一块儿，<code>Vite</code> 有自己的一套处理逻辑。</p><p>在 <code>dev server</code> 内部，有一个缓存对象 - <code>moduleGraph</code>, 用来缓存请求过的文件内容和对应的 <code>ETag</code> 信息。</p><p>当 <code>dev server</code> 启动以后，这个对象是空的。此时如果浏览器发起业务代码请求，<code>dev server</code> 需要根据请求，将请求 <code>url</code> 解析为文件的本地路径(<code>resolve</code>)、读取文件内容(<code>load</code>)、对文件内容做转换(<code>transform</code> - 如 <code>less</code> 转换为 <code>css</code>)、计算 <code>ETag</code>，并缓存到 <code>moduleGraph</code> 中。 此时，如果请求中 <code>If-None-Match</code> 字段中携带的 <code>ETag</code> 信息和新计算的 <code>ETag</code> 信息一致，<code>server</code> 端会将状态码设置为 <code>304</code>，不返回转换以后的内容。</p><p>当浏览器再次请求相同文件时，<code>dev server</code> 会根据请求路径直接从缓存中去读取文件内容和 <code>ETag</code> 信息，然后和请求中 <code>If-None-Match</code> 字段中携带的 <code>ETag</code> 信息做比较。如果一致，<code>server</code> 端直接将状态码设置为 <code>304</code>，通知浏览器使用本地缓存。</p><p>了解了这些，那么首次访问应用时协商缓存为什么没有起作用就好理解了。因为这个时候，<code>dev server</code> 内部的缓存为空，所有的请求都需要经历 <code>resolve</code>、<code>load</code>、<code>transform</code> 的过程，导致响应耗时较久。</p><p>做一个简单的总结:</p><ul><li>开发模式下，请求预加载文件采用强缓存策略，请求业务文件采用协商缓存策略。</li><li>首次访问应用请求业务文件时，尽管会命中协商缓存，但 <code>sever</code> 端依旧会进行 <code>resolve</code>、<code>load</code>、<code>transform</code> 操作，导致响应需要消耗一定的时间。</li><li>二次访问应用，强缓存和协商缓存同时起作用，性能很好。</li></ul><p>其实，关于结论中提到的第二点，我们还是有办法进行优化的，比如说采取 <code>Webpack5</code> 的缓存策略，在 <code>dev server</code> 关闭之前将 <code>moduleGraph</code> 缓存到本地，等到再次启动时读取本地缓存。</p><h2 id="vite插件" tabindex="-1"><a class="header-anchor" href="#vite插件" aria-hidden="true">#</a> Vite插件</h2><h5 id="_1-vite-独有的钩子" tabindex="-1"><a class="header-anchor" href="#_1-vite-独有的钩子" aria-hidden="true">#</a> 1. vite 独有的钩子</h5><ol><li><code>enforce</code> ：值可以是<code>pre</code> 或 <code>post</code> ， <code>pre</code> 会较于 <code>post</code> 先执行；</li><li><code>apply</code> ：值可以是 <code>build</code> 或 <code>serve</code> 亦可以是一个函数，指明它们仅在 <code>build</code> 或 <code>serve</code> 模式时调用；</li><li><code>config(config, env)</code> ：可以在 vite 被解析之前修改 vite 的相关配置。钩子接收原始用户配置 config 和一个描述配置环境的变量env；</li><li><code>configResolved(resolvedConfig)</code> ：在解析 vite 配置后调用。使用这个钩子读取和存储最终解析的配置。当插件需要根据运行的命令做一些不同的事情时，它很有用。</li><li><code>configureServer(server)</code> ：主要用来配置开发服务器，为 dev-server (connect 应用程序) 添加自定义的中间件；</li><li><code>transformIndexHtml(html)</code> ：转换 index.html 的专用钩子。钩子接收当前的 HTML 字符串和转换上下文；</li><li><code>handleHotUpdate(ctx)</code>：执行自定义HMR更新，可以通过ws往客户端发送自定义的事件；</li></ol><h5 id="_2-vite-与-rollup-的通用钩子之构建阶段" tabindex="-1"><a class="header-anchor" href="#_2-vite-与-rollup-的通用钩子之构建阶段" aria-hidden="true">#</a> 2. vite 与 rollup 的通用钩子之构建阶段</h5><ol><li><code>options(options)</code> ：在服务器启动时被调用：获取、操纵Rollup选项，严格意义上来讲，它执行于属于构建阶段之前；</li><li><code>buildStart(options)</code>：在每次开始构建时调用；</li><li><code>resolveId(source, importer, options)</code>：在每个传入模块请求时被调用，创建自定义确认函数，可以用来定位第三方依赖；</li><li><code>load(id)</code>：在每个传入模块请求时被调用，可以自定义加载器，可用来返回自定义的内容；</li><li><code>transform(code, id)</code>：在每个传入模块请求时被调用，主要是用来转换单个模块；</li><li><code>buildEnd(error?: Error)</code>：在构建阶段结束后被调用，此处构建结束只是代表所有模块转义完成；</li></ol><h5 id="_3-vite-与-rollup-的通用钩子之输出阶段" tabindex="-1"><a class="header-anchor" href="#_3-vite-与-rollup-的通用钩子之输出阶段" aria-hidden="true">#</a> 3. vite 与 rollup 的通用钩子之输出阶段</h5><ol><li><code>outputOptions(options)</code>：接受输出参数；</li><li><code>renderStart(outputOptions, inputOptions)</code>：每次 bundle.generate 和 bundle.write 调用时都会被触发；</li><li><code>augmentChunkHash(chunkInfo)</code>：用来给 chunk 增加 hash；</li><li><code>renderChunk(code, chunk, options)</code>：转译单个的chunk时触发。rollup 输出每一个chunk文件的时候都会调用；</li><li><code>generateBundle(options, bundle, isWrite)</code>：在调用 bundle.write 之前立即触发这个 hook；</li><li><code>writeBundle(options, bundle)</code>：在调用 bundle.write后，所有的chunk都写入文件后，最后会调用一次 writeBundle；</li><li><code>closeBundle()</code>：在服务器关闭时被调用</li></ol><p><img src="'+s+'" alt="img"></p><p>重点就是<code>fs</code>（文件系统）和<code>path</code>（文件路径）两个模块</p><p>用<code>fs.readdirSync</code> 读取文件夹/文件 然后筛选<code>isFile</code></p><p>对文件夹进行递归调用 文件放入routeOption对象里面</p><p>先注册虚拟模块，autoRoute，然后在resolveId中捕获该模块（不然会报错，因为虚拟模块不对应实际文件，是在内存中生存的，在构建过程中动态创建和处理）</p><p>在configServer里面来进行操作，configSerser接受的参数是dev server实例，然后server.middlewares.use，在这当中调用生成路由的函数并response即可。</p><p>Vite的首评加载会很慢，可以通过prefetch来优化</p>',59),u={href:"https://juejin.cn/post/7125753214489591821",target:"_blank",rel:"noopener noreferrer"},h=o(`<h4 id="webpack-rollup-parcel" tabindex="-1"><a class="header-anchor" href="#webpack-rollup-parcel" aria-hidden="true">#</a> Webpack / Rollup / Parcel</h4><p><code>Webpack</code>、<code>Rollup</code>、<code>Parcel</code> 统称为<code>静态模块打包器</code>。</p><p>这一类构建工具，通常需要指定入口 - <code>entry</code>，然后以 <code>entry</code> 为起点，通过分析整个项目内各个源文件之间的依赖关系，构建一个模块依赖图 - <code>module graph</code>，然后再将 <code>module graph</code> 分离为三种类型的 <code>bundle</code>: <code>entry</code> 所在的 <code>initial bundle</code>、<code>lazy load</code> 需要的 <code>async bundle</code> 和自定义分离规则的 <code>custome bundle</code>。</p><p>这几个构建工具各有优势:</p><ul><li><code>Webpack</code> 大而全，配置灵活，生态丰富，是构建工具的首选。</li><li><code>Parcel</code> 号称零配置，使用简单，适合不太需要定制化构建的项目使用。</li><li><code>Rollup</code> 推崇 <code>ESM</code> 标准开发，打包出来的代码干净，适用于组件库开发。</li></ul><h4 id="vite-esbuild" tabindex="-1"><a class="header-anchor" href="#vite-esbuild" aria-hidden="true">#</a> Vite / Esbuild</h4><p>新一代构建工具。</p><p><code>esbuild</code>, 基于 <code>go</code> 语言实现，代码直接编译成机器码(不用像 js 那样先解析为字节码，再编译为机器码)，构建速度比 <code>webpack</code> 更快。</p><p><code>vite</code>, 开发模式下借助浏览器对 <code>ESM</code> 的支持，采用 <code>nobundle</code> 的方式进行构建，能提供极致的开发体验；生产模式下则基于 <code>rollup</code> 进行打包构建。</p><blockquote><p><code>Webpack</code> 是一个静态模块打包器，它会以 <code>entry</code> 指定的入口文件为起点，分析整个项目内各个源文件之间的依赖关系，构建一个模块依赖图 - <code>module graph</code>，然后将 <code>module graph</code> 分离为多个 <code>bundle</code>。在构建 <code>module graph</code> 的过程中，会使用 <code>loader</code> 处理源文件，将它们转化为浏览器可以是识别的 <code>js</code>、<code>css</code>、<code>image</code>、<code>音视频</code>等。</p></blockquote><h3 id="初探-vite-预构建" tabindex="-1"><a class="header-anchor" href="#初探-vite-预构建" aria-hidden="true">#</a> 初探 Vite 预构建</h3><p>使用过 <code>Vite</code> 的同学都知道，开发阶段 <code>Vite</code> 会对项目中使用的第三方依赖如 <code>react</code>、<code>react-dom</code>、<code>lodash-es</code> 等做<code>预构建</code>操作。</p><p>之所以要做<code>预构建</code>，是因为 <code>Vite</code> 是基于浏览器原生的 <code>ESM</code> 规范来实现 <code>dev server</code> 的，这就要求整个项目中涉及的所有源代码必须符合 <code>ESM</code> 规范。</p><p>而在实际开发过程中，业务代码我们可以严格按照 <code>ESM</code> 规范来编写，但第三方依赖就无法保证了，比如 <code>react</code>。这就需要我们通过 <code>Vite</code> 的<code>预构建</code>功能将非 <code>ESM</code> 规范的代码转换为符合 <code>ESM</code> 规范的代码。</p><p>另外，尽管有些第三方依赖已经符合 <code>ESM</code> 规范，但它是由多个子文件组成的，如 <code>lodash-es</code>。如果不做处理就直接使用，那么就会引发请求瀑布流，这对页面性能来说，简直就是一场灾难。同样的，我们可以通过 <code>Vite</code> 的<code>预构建</code>功能，将第三方依赖内部的多个文件合并为一个，减少 <code>http</code> 请求数量，优化页面加载性能。</p><p>综上，<code>预构建</code>，主要做了两件事情：</p><ul><li>将非 <code>ESM</code> 规范的代码转换为符合 <code>ESM</code> 规范的代码；</li><li>将第三方依赖内部的多个文件合并为一个，减少 <code>http</code> 请求数量；</li></ul><h3 id="二次预构建" tabindex="-1"><a class="header-anchor" href="#二次预构建" aria-hidden="true">#</a> 二次预构建</h3><p><code>预构建</code>，最关键的一步就是找到项目中所有的第三方依赖。 那 <code>Vite</code> 是怎么做到快速获取项目中所有的第三方依赖呢？</p><p>在解释这个问题之前，我们先来聊一聊 <code>Webpack</code>、<code>Rollup</code>、<code>Parcel</code> 这一类<code>静态打包器</code>是如何打包代码的。以 <code>Webpack</code> 为例，整个打包过程可以分为构建模块依赖图 - <code>module graph</code> 和将 <code>module graph</code> 分离为多个 <code>bundles</code> 两个步骤。其中，构建 <code>module graph</code> 是重中之重。</p><p><code>Webpack</code> 构建 <code>module graph</code> 的过程，可以拆解成下面几个步骤：</p><ol><li>找到入口文件 <code>entry</code> 对应的 <code>url</code>, 这个 <code>url</code> 一般为相对路径；</li><li>将 <code>url</code> 解析为绝对路径，找到源文件在本地磁盘的位置，并构建一个 <code>module</code> 对象；</li><li>读取源文件的内容；</li><li>将源文件内容解析为 <code>AST</code> 对象，分析 <code>AST</code> 对象，找到源文件中的<code>静态依赖</code>(import xxx from &#39;xxx&#39;) 和<code>动态依赖</code>(import(&#39;xx&#39;))对应的 <code>url</code>, 并收集到 <code>module</code> 对象中；</li><li>遍历第 4 步收集到的<code>静态依赖</code>、<code>动态依赖</code>对应的 <code>url</code>，重复 2 - 5 步骤，直到项目中所有的源文件都遍历完成。</li></ol><p>在构建 <code>module graph</code> 过程中，我们就可以知道整个项目涉及的所有源文件对应的 <code>url</code>，然后就可以从这些 <code>url</code> 中过滤出第三方依赖。</p><p>同样的，<code>Vite</code> 在<code>预构建</code>的时候也是基于类似的机制去找到项目中所有的第三方依赖的。和 <code>Webpack</code> 不同， <code>Vite</code> 另辟蹊径，借助了 <code>Esbuild</code> 比 <code>Webpack</code> 更快的打包能力，对整个项目做一个全量打包。打包的时候，通过分析<code>依赖关系</code>，得到项目中所有的源文件的 <code>url</code>，然后分离出第三方依赖。</p><p>这样，<code>Vite</code> 就可以对找到的第三方依赖做转化、合并操作了。</p><p><code>预构建</code>功能非常棒，但在实际的项目中，并不能保证所有的第三方依赖都可以被找到。如果出现下面的这两种情况， <code>Esbuild</code> 也无能为力:</p><ul><li><code>plugin</code> 在运行过程中，动态给源码注入了新的第三方依赖；</li><li><code>动态依赖</code>在代码运行时，才可以确定最终的 <code>url</code>；</li></ul><p>当出现这两种情况时，<code>Vite</code> 会触发<code>二次预构建</code>。</p><h3 id="_3-0的优化" tabindex="-1"><a class="header-anchor" href="#_3-0的优化" aria-hidden="true">#</a> 3.0的优化</h3><p><code>3.0</code> 版本对第三方依赖的请求和业务代码的请求有不同的处理逻辑。当浏览器请求业务代码时，<code>dev server</code> 只要完成源代码转换并收集到依赖模块的 <code>url</code>，就会给浏览器发送 <code>response</code>。而第三方依赖请求则不同，<code>dev server</code> 会等首屏期间涉及的所有模块的依赖关系全部解析完毕以后，才会给浏览器发送 <code>response</code>。这就导致了，如果发现有未<code>预构建</code>的第三方依赖，第三方依赖的请求会一直被阻塞，直到<code>二次预构建</code>完成为止。</p><p>有了这种操作，当然就不需要 <code>reload</code> 操作了。</p><h2 id="vite-的快" tabindex="-1"><a class="header-anchor" href="#vite-的快" aria-hidden="true">#</a> Vite 的快</h2><p><code>Vite</code> 的快，主要体现在两个方面: 快速的冷启动和快速的热更新。而 <code>Vite</code> 之所以能有如此优秀的表现，完全归功于 <code>Vite</code> 借助了浏览器对 <code>ESM</code> 规范的支持，采取了与 <code>Webpack</code> 完全不同的 <code>unbundle</code> 机制。</p><h4 id="快速的冷启动" tabindex="-1"><a class="header-anchor" href="#快速的冷启动" aria-hidden="true">#</a> 快速的冷启动</h4><p>就是启动项目的时间，大伙都知道Vite比webpack要快不少。这个问题，主要是由 <code>Webpack</code> 内部的核心机制 - <code>bundle</code> 模式引发的。</p><p><code>Webpack</code> 能大行其道，归功于它划时代的采用了 <code>bundle</code> 机制。通过这种 <code>bundle</code> 机制，<code>Webpack</code> 可以将项目中各种类型的源文件转化供浏览器识别的 <code>js</code>、<code>css</code>、<code>img</code> 等文件，建立源文件之间的依赖关系，并将数量庞大的源文件合并为少量的几个输出文件。</p><p><code>bundle</code> 工作机制的核心部分分为两块：构建模块依赖图 - <code>module graph</code> 和将 <code>module graph</code> 分解为最终供浏览器使用的几个输出文件。</p><p>构建 <code>module graph</code> 的过程可以简单归纳为:</p><ol><li>获取配置文件中 <code>entry</code> 对应的 <code>url</code> (这个 <code>url</code> 一般为相对路径);</li><li><code>resolve</code> - 将 <code>url</code> 解析为绝对路径，找到源文件在本地磁盘的位置，并构建一个 <code>module</code> 对象；</li><li><code>load</code> - 读取源文件的内容;</li><li><code>transform</code> - 使用对应的 <code>loader</code> 将源文件内容转化为浏览器可识别的类型；</li><li><code>parse</code> - 将转化后的源文件内容解析为 <code>AST</code> 对象，分析 <code>AST</code> 对象，找到源文件中的静态依赖(<code>import xxx from &#39;xxx&#39;</code>) 和动态依赖(<code>import(&#39;xx&#39;)</code>)对应的 <code>url</code>, 并收集到 <code>module</code> 对象中；</li><li>遍历第 <code>5</code> 步收集到的静态依赖、动态依赖对应的 <code>url</code>，重复 <code>2</code> - <code>6</code> 步骤，直到项目中所有的源文件都遍历完成。</li></ol><p>分解 <code>module graph</code> 的过程也可以简单归纳为:</p><ol><li>预处理 <code>module graph</code>，对 <code>module graph</code> 做 <code>tree shaking</code>；</li><li>遍历 <code>module graph</code>，根据静态、动态依赖关系，将 <code>module graph</code> 分解为 <code>initial chunk</code>、<code>async chunks</code>；</li><li>优化 <code>initial chunk</code>、 <code>async chunks</code> 中重复的 <code>module</code>；</li><li>根据 <code>optimization.splitChunks</code> 进行优化，分离第三方依赖、被多个 <code>chunk</code> 共享的 <code>module</code> 到 <code>common chunks</code> 中；</li><li>根据 <code>chunk</code> 类型，获取对应的 <code>template</code>；</li><li>遍历每个 <code>chunk</code> 中收集的 <code>module</code>，结合 <code>template</code>，为每个 <code>chunk</code> 构建最后的输出内容；</li><li>将最后的构建内容输出到 <code>output</code> 指定位置；</li></ol><p><code>Webpack</code> 的这种 <code>bundle</code> 机制，奠定了现代静态打包器(如 <code>Rollup</code>、<code>Parcel</code>、<code>Esbuild</code>)的标准工作模式。</p><p>然而成也萧何败萧何，强大的 <code>bundle</code> 机制，也引发了构建速度缓慢的问题，而且项目规模越大，构建速度越是缓慢。其主要原因是构建 <code>module graph</code> 的过程中，涉及到大量的文件 <code>IO</code>、文件 <code>transfrom</code>、文件 <code>parse</code> 操作；以及分解 <code>module graph</code> 的过程中，需要遍历 <code>module graph</code>、文件 <code>transform</code>、文件 <code>IO</code> 等。这些操作，往往需要消耗大量的时间，导致构建速度变得缓慢。</p><p>开发模式下，<code>dev server</code> 需要 <code>Webpack</code> 完成整个工作链路才可以启动成功，这就导致构建过程耗时越久，<code>dev server</code> 启动越久。</p><p>为了加快构建速度，<code>Webpack</code> 也做了大量的优化，如 <code>loader</code> 的缓存功能、<code>webpack5</code> 的持久化缓存等，但这些都治标不治本，只要 <code>Webpack</code> 的核心工作机制不变，那 <code>dev server</code> 启动优化，依旧是一个任重道远的过程(基本上永远都达不到 <code>Vite </code>那样的效果)。</p><p>而Vite采取了与 <code>Webpack</code> 截然不同的 <code>unbundle</code> 机制。</p><p><code>unbundle</code> 机制，顾名思义，不需要做 <code>bundle</code> 操作，即不需要构建、分解 <code>module graph</code>，源文件之间的依赖关系完全通过浏览器对 <code>ESM</code> 规范的支持来解析。这就使得 <code>dev server</code> 在启动过程中只需做一些初始化的工作，剩下的完全由浏览器支持</p><ul><li><p><code>unbundle</code> 机制，顾名思义，不需要做 <code>bundle</code> 操作，即不需要构建、分解 <code>module graph</code>，源文件之间的依赖关系完全通过浏览器对 <code>ESM</code> 规范的支持来解析。这就使得 <code>dev server</code> 在启动过程中只需做一些初始化的工作，剩下的完全由浏览器支持。这和 <code>Webpack</code> 的 <code>bundle</code> 机制一比，简直就是降维打击，都有点欺负人了 。</p><p>那有的同学就会问，源文件的 <code>resolve</code>、<code>load</code>、<code>transform</code>、<code>parse</code> 什么时候做呢 ？</p><p>答案是浏览器发起请求以后，<code>dev server</code> 端会通过 <code>middlewares</code> 对请求做拦截，然后对源文件做 <code>resolve</code>、<code>load</code>、<code>transform</code>、<code>parse</code> 操作，然后再将转换以后的内容发送给浏览器。</p><p>这样，通过 <code>unbundle</code> 机制， <code>Vite</code> 便可以在 <code>dev server</code> 启动方面获取远超于 <code>Webpack</code> 的优秀体验。</p><p>最后再总结一下， <code>unbundle</code> 机制的核心:</p><ul><li>模块之间的依赖关系的解析由浏览器实现；</li><li>文件的转换由 <code>dev server</code> 的 <code>middlewares</code> 实现并做缓存；</li><li>不对源文件做合并捆绑操作；</li></ul></li></ul><h4 id="快速的热更新" tabindex="-1"><a class="header-anchor" href="#快速的热更新" aria-hidden="true">#</a> 快速的热更新</h4><p>Webpack:</p><p><code>dev server</code> 启动以后，会 <code>watch</code> 源文件的变化。当源文件发生变化后，<code>Webpack</code> 会重新编译打包。这个时候，由于我们只修改了一个文件，因此只需要对这个源文件做 <code>resolve</code>、 <code>load</code>、 <code>transfrom</code>、<code>parse</code> 操作，依赖的文件直接使用缓存，因此 <code>dev server</code> 的响应速度比冷启动要好很多。</p><p><code>dev server</code> 重新编译打包以后，会通过 <code>ws</code> 连接通知浏览器去获取新的打包文件，然后对页面做局部更新。</p><p>Vite:</p><p>由于 <code>Vite</code> 采用 <code>unbundle</code> 机制，所以 <code>dev server</code> 在监听到文件发生变化以后，只需要通过 <code>ws</code> 连接通知浏览器去重新加载变化的文件，剩下的工作就交给浏览器去做了。</p><h2 id="vite的慢" tabindex="-1"><a class="header-anchor" href="#vite的慢" aria-hidden="true">#</a> Vite的慢</h2><p>和 <code>bundle</code> 机制有利有弊一样，<code>unbundle</code> 机制给 <code>Vite</code> 在 <code>dev server</code> 方面获得巨大性能提升的同时，也带来一些负面影响，那就是<code>首屏</code>和<code>懒加载</code>性能的下降</p><p>Webpack:</p><p>浏览器向 <code>dev server</code> 发起请求， <code>dev server</code> 接受到请求，然后将已经打包构建好的首屏内容发送给浏览器。整个过程非常普遍，没有什么可说的，不存在什么性能问题。</p><p>Vite:</p><p>由于 <code>unbundle</code> 机制，首屏期间需要额外做以下工作:</p><ul><li>不对源文件做合并捆绑操作，导致大量的 <code>http</code> 请求；</li><li><code>dev server</code> 运行期间对源文件做 <code>resolve</code>、<code>load</code>、<code>transform</code>、<code>parse</code> 操作；</li><li>预构建、二次预构建操作也会阻塞首屏请求，直到预构建完成为止；</li></ul><p>和 <code>Webpack</code> 对比，<code>Vite</code> 把需要在 <code>dev server</code> 启动过程中完成的工作，转移到了 <code>dev server</code> 响应浏览器请求的过程中，不可避免的导致首屏性能下降。</p><p>不过首屏性能差只发生在 <code>dev server</code> 启动以后第一次加载页面时发生。之后再 <code>reload</code> 页面时，首屏性能会好很多。原因是 <code>dev server</code> 会将之前已经完成转换的内容缓存起来。</p><p>和首屏一样，由于 <code>unbundle</code> 机制，动态加载的文件，需要做 <code>resolve</code>、<code>load</code>、<code>transform</code>、<code>parse</code> 操作，并且还有大量的 <code>http</code> 请求，导致懒加载性能也受到影响。</p><p>此外，如果懒加载过程中，发生了二次预构建，页面会 <code>reload</code>，对开发体验也有一定程度的影响</p><h3 id="esbuild" tabindex="-1"><a class="header-anchor" href="#esbuild" aria-hidden="true">#</a> Esbuild</h3><p>我们提到过开发模式下使用 <code>Vite</code> 会有首屏性能下降的负面效果。之所以会造成首屏性能下降，一方面是 <code>dev server</code> 需要完成预构建才可以响应首屏请求；另一方面是需要对请求文件做实时转换。</p><blockquote><p>我们都知道 <code>vite</code> 本身使用的 <code>rollup</code> 打包，<code>vite</code> 的插件也和 <code>rollup</code> 的插件机制相吻合； <code>esbuild</code> 是用于在开发环境中对文件进行处理，也有自己的声明周期钩子函数，由于对文件的分割和 <code>css</code> 支持还不太友好，暂未应用到打包环节。</p></blockquote><p>那么Vite是如何利用Esbuild提升性能的？</p><p>首先我们要知道，Esbuild要比一般的打包器要快很多</p><p>而 <code>Esbuild</code> 之所以能这么快，主要原因有两个:</p><ul><li><p><code>Go</code> 语言开发，可以多线程打包，代码直接编译成机器码；</p><p><code>Webpack</code> 一直被人诟病构建速度慢，主要原因是在打包构建过程中，存在大量的 <code>resolve</code>、<code>load</code>、<code>transform</code>、<code>parse</code> 操作，而这些操作通常是通过 <code>javascript</code> 代码来执行的。要知道，<code>javascript</code> 并不是什么高效的语言，在执行过程中要先编译后执行，还是单线程并且不能利用多核 <code>cpu</code> 优势，和 <code>Go</code> 语言相比，效率很低。</p></li><li><p>可充分利用多核 <code>cpu</code> 优势；</p></li></ul><h4 id="关键-api-transfrom-build" tabindex="-1"><a class="header-anchor" href="#关键-api-transfrom-build" aria-hidden="true">#</a> 关键 API - transfrom &amp; build</h4><p><code>Esbuild</code> 并不复杂。它对外提供了两个 <code>API</code> - <code>transform</code> 和 <code>build</code>，使用起来非常简单。</p><p><code>transfrom</code>，转换的意思。通过这个 api，我们可以将 <code>ts</code>、<code>jsx</code>、<code>tsx</code> 等格式的内容转化为 <code>js</code>。 <code>transfrom</code> 只负责文件内容转换，并不会生成一个新的文件。</p><p><code>build</code>，构建的意思，根据指定的单个或者多个入口，分析依赖，并使用 <code>loader</code> 将不同格式的内容转化为 js 内容，生成一个或多个 <code>bundle</code> 文件。</p><p><code>Esbuild</code> 对外提供的 <code>hook</code> 比较简单，总共 <code>4</code> 个:</p><ul><li><code>onResolve</code>, 解析 <code>url</code> 时触发，可自定义 <code>url</code> 如何解析。如果 <code>callback</code> 有返回 <code>path</code>，后面的同类型 <code>callback</code> 将不会执行。所有的 <code>onResolve</code> <code>callback</code> 将按照对应的 <code>plugin</code> 注册的顺序执行。</li><li><code>onLoad</code>, 加载模块时触发，可自定义模块如何加载。 如果 <code>callback</code> 有返回 <code>contents</code>，后面的同类型 <code>callback</code> 将不会执行。所有的 <code>onLoad</code> <code>callback</code> 将按照对应的 <code>plugin</code> 注册的顺序执行。</li><li><code>onStart</code>, 每次 <code>build</code> 开始时都会触发，没有入参，因此不具有改变 <code>build</code> 的能力。多个 <code>plugin</code> 的 <code>onStart</code> 并行执行。</li><li><code>onEnd</code>, 每次 <code>build</code> 结束时会触发，入参为 <code>build</code> 的结果，可对 <code>result</code> 做修改。所有的的 <code>onEnd</code> 将按照对应的 <code>plugin</code> 注册的顺序执行。</li></ul><p>正是有了 <code>onResolve</code>、<code>onLoad</code>、<code>onStart</code>、<code>onEnd</code>，我们可以在 <code>build</code> 过程中的解析 <code>url</code>、加载模块内容、构建开始、构建结束阶段介入，做自定义操作。</p><h4 id="预构建" tabindex="-1"><a class="header-anchor" href="#预构建" aria-hidden="true">#</a> 预构建</h4><p>先来回顾一下为什么要做预构建。</p><p>原因有两点:</p><ul><li>将非 <code>ESM</code> 规范的代码转换为符合 <code>ESM</code> 规范的代码；</li><li>将第三方依赖内部的多个文件合并为一个，减少 <code>http</code> 请求数量；</li></ul><p>要完成预构建，最关键的两点是找到项目中所有的第三份依赖和对第三方依赖做合并、转换。借助 <code>Esbuild</code>，<code>Vite</code> 很轻松的实现了这两个诉求。</p><ul><li><p><strong>寻找第三方依赖</strong></p><p>寻找第三方依赖的过程非常简单，分为两步:</p><ol><li>定义一个带 <code>onResolve hook</code> 和 <code>onLoad hook</code> 的 <code>esbuild plugin</code>；</li><li>执行 <code>esbuild</code> 的 <code>build</code> 方法做打包构建；</li></ol><p>和 <code>Webpack</code>、<code>Rollup</code>、<code>Parcel</code> 等构建工具一样，<code>Esbuild</code> 在做打包构建时也要构建模块依赖图 - <code>module graph</code></p><p>在构建 <code>module graph</code> 时，第一步就是解析模块的绝对路径，这个时候就会触发 <code>onResolve hook</code>。在 <code>onResolve hook</code> 触发时，会传入模块的路径。根据模块的路径，我们就可以判断出这个模块是第三方依赖还是业务代码。</p><p>举个 🌰，</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// main.tsx</span>

<span class="token keyword">import</span> react <span class="token keyword">from</span> <span class="token string">&#39;react&#39;</span><span class="token punctuation">;</span>
<span class="token keyword">import</span> CustomeComponent <span class="token keyword">from</span> <span class="token string">&#39;./components/CustomeComponent&#39;</span><span class="token punctuation">;</span>
<span class="token operator">...</span>
复制代码
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在对 <code>main.tsx</code> 的内容做 <code>parser</code> 操作时，能知道 <code>main.tsx</code> 依赖 <code>react</code> 和 <code>CustomeComponent</code>，然后开始解析 <code>react</code> 和 <code>CustomeComponent</code>。</p><p>解析 <code>react</code>、<code>CustomeComponent</code> 时，会触发 <code>onResolve hook</code>，入参分别为 <code>&#39;react&#39;</code> 和 <code>&#39;./components/CustomeComponent&#39;</code>。根据入参，我们可以很清楚的区分 <code>&#39;react&#39;</code> 是第三方依赖，<code>&#39;./components/CustomeComponet&#39;</code> 是业务代码。</p><p>这样，<code>esbuild</code> 完成构建，项目中的第三方依赖也就收集完毕了。所有的第三方依赖会收集到一个 <code>deps</code> 列表中。</p></li><li><p><strong>合并、转换第三方依赖</strong></p><p>知道了项目中的第三方依赖以后，再做合并、转换操作就非常简单了。</p><p>这一步， <code>Vite</code> 直接通过 <code>esbuild</code> 提供的 <code>build</code> 方法，指定 <code>entryPoints</code> 为收集到的第三方依赖，<code>format</code> 为 <code>esm</code>，再做一次打包构建。</p><p>这一次，会对第三方依赖做合并、转换操作。打包构建完成以后，再把构建内容输出到 <code>/node_modules/.vite/deps</code> 下。</p></li></ul><p>这样，通过两次 <code>esbuild.build</code>，预构建就完成了。</p><h4 id="middlewares-中内容转换" tabindex="-1"><a class="header-anchor" href="#middlewares-中内容转换" aria-hidden="true">#</a> middlewares 中内容转换</h4><p><code>Vite</code> 中源文件的转换是在 <code>dev server</code> 启动以后通过 <code>middlewares</code> 实现的。</p><p>当浏览器发起请求以后，<code>dev sever</code> 会通过相应的 <code>middlewares</code> 对请求做处理，然后将处理以后的内容返回给浏览器。</p><p><code>middlewares</code> 对源文件的处理，分为 <code>resolve</code>、<code>load</code>、<code>transform</code>、<code>parser</code> 四个过程：</p><ol><li><p><code>resolve</code> - 解析 <code>url</code>，找到源文件的绝对路径；</p></li><li><p><code>load</code> - 加载源文件。如果是第三方依赖，直接将预构建内容返回给浏览器；如果是业务代码，继续 <code>transform</code>、<code>parser</code>。</p></li><li><p><code>transfrom</code> - 对源文件内容做转换，即 <code>ts</code> -&gt; <code>js</code>, <code>less</code> -&gt; <code>css</code> 等。转换完成的内容可以直接返回给浏览器了。</p></li><li><p><code>parser</code> - 对转换以后的内容做分析，找到依赖模块，对依赖模块做预转换 - <code>pre transform</code> 操作，即重复 <code>1</code> - <code>4</code>。</p><p><code>pre transform</code> 是 <code>Vite</code> 做的一个优化点。预转换的内容会先做缓存，等浏览器发起请求以后，如果已经完成转换，直接将缓存的内容返回给浏览器。</p></li></ol><p><code>Vite</code> 在处理步骤 <code>3</code> 时，是通过 <code>esbuild.transform</code> 实现的，对比 <code>Webpack</code> 使用各个 <code>loader</code> 处理源文件，那是非常简单、快捷的。</p><h3 id="prefetch优化懒加载" tabindex="-1"><a class="header-anchor" href="#prefetch优化懒加载" aria-hidden="true">#</a> Prefetch优化懒加载</h3>`,93),b={href:"https://juejin.cn/post/7131636427183357965",target:"_blank",rel:"noopener noreferrer"},v=o('<h3 id="为什么懒加载性能会差" tabindex="-1"><a class="header-anchor" href="#为什么懒加载性能会差" aria-hidden="true">#</a> 为什么懒加载性能会差</h3><p>首先我们先来解释一下为什么通过 <code>Vite</code> 启动 <code>dev server</code> 以后，懒加载性能会差(注意：性能差仅限于 <code>dev server</code> 启动以后首次打开应用的某一个页面)。</p><p>原因有两点：</p><ul><li>大量的 <code>http</code> 请求；</li><li><code>dev server</code> 实时对浏览器请求的文件做 <code>transform</code>；</li></ul><p>由于 <code>Vite</code> 采取了 <code>unbundle</code> 机制，源文件并没有像 <code>webpack</code> 那样做合并捆绑，这就导致了不管是首屏还是懒加载，都会因为请求 <code>js</code>、<code>css</code> 等资源产生大量的 <code>http</code> 请求。另外，在请求过程中，<code>dev server</code> 需要对源文件做实时转换。其中，实时转换源文件是影响最大的。</p><p>这两种情形，和我们常用的优化手段如减少 <code>http</code> 请求、提前对源文件做处理背道而驰，也就导致了懒加载性能下降。</p><p>知道了性能下降的原因，那我们就可以针对性的做性能优化了。</p><p><code>http</code> 请求数量多这个问题，影响较小，因此小编只重点关注源文件实时处理这个问题。解决方案也很简单，就是在用户点击某个页面之前，提前向 <code>dev server</code> 发起请求，让 <code>dev server</code> 提前去对请求文件做 <code>transform</code>,等到真正需要加载某个资源时，直接返回已经 <code>transform</code> 的资源。</p><p>这种手段也就是我们常说的 <code>prefetch</code>。</p>',9);function m(k,f){const d=p("ExternalLinkIcon");return r(),a("div",null,[n,e("p",null,[e("a",u,[c("参考"),l(d)])]),h,e("p",null,[e("a",b,[c("参考"),l(d)])]),v])}const x=i(t,[["render",m],["__file","vite.html.vue"]]);export{x as default};
